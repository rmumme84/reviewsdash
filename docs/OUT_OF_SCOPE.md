# acker-site — Itens fora do escopo (tracking)

Este arquivo lista pendências/ideias que apareceram durante o trabalho do `acker-site`, mas **não pertencem** ao escopo `/opt/acker-site` (ou dependem do humano).

## Bloqueados (precisam do humano)
- Preencher `USER.md` do agente (nome/como chamar/timezone).

## Fora do escopo do acker-site
- (Series iPhone) Ajuste final de layout do card (padding/altura/score overlay + chips + botões harmonizados).

> Se quiser, mova esses itens para o backlog do projeto correspondente.
